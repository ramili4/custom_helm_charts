apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zeebe-node-watcher.fullname" . }}-test-connection"
  labels:
    {{- include "zeebe-node-watcher.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zeebe-node-watcher.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
