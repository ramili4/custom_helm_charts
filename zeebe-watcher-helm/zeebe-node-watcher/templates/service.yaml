apiVersion: v1
kind: Service
metadata:
  name: {{ include "zeebe-node-watcher.fullname" . }}
  labels:
    {{- include "zeebe-node-watcher.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zeebe-node-watcher.selectorLabels" . | nindent 4 }}
